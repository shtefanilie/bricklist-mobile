{
  "mcpServers": {
    "expo": {
      "command": "npx",
      "args": ["--no-install", "expo-mcp", "--dev-server-url", "http://localhost:8081", "--root", "app"]
    },
    "argent": {
      "command": "npx",
      "args": ["--no-install", "argent", "mcp"]
    }
  }
}
