# BrickList Workshop App

Mobile-only Expo app for physical iOS and Android devices using Expo Go.

## Run

```bash
npm install
npm run ios
# or
npm run android
```

Configure the Worker endpoint and workshop API key using `.env.example` before launching.
